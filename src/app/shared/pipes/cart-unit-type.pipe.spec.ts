import { CartUnitTypePipe } from './cart-unit-type.pipe';

describe('CartUnitTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CartUnitTypePipe();
    expect(pipe).toBeTruthy();
  });
});
