import { CartPricePipe } from './cart-price.pipe';

describe('CartPricePipe', () => {
  it('create an instance', () => {
    const pipe = new CartPricePipe();
    expect(pipe).toBeTruthy();
  });
});
